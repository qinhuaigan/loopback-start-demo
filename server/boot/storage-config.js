module.exports = function(app) {
  // app.dataSources.File.connector.getFilename = function(uploadingFile, req, res) {
  //   console.log('重命名文件')
  //   console.log('uploadingFile ====', uploadingFile)
  //   return Math.random().toString().substr(2) + '.jpg';
  // };
};
